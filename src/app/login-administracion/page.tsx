import { LoginForm } from "@/auth"

function LoginPage() {
  return (
    <main className="flex items-center justify-center h-[100dvh] px-4 lg:px-0">
      <LoginForm />
    </main>
  )
}

export default LoginPage